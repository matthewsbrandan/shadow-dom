# SHADOW DOM

![home](home.png)

  - A Shadow Dom nos ajuda a criar webcomponents, se a utilização de framework. Isso serve para podermos utilizar o conceitos de encapsulamento, e assim criar componentes(ou novas tags HTML) que possuem comportamento(com ações JS internas) e seu próprio CSS.
  